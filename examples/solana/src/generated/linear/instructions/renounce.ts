/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { SABLIER_LOCKUP_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const RENOUNCE_DISCRIMINATOR = new Uint8Array([
  241, 157, 138, 210, 8, 235, 187, 123,
]);

export function getRenounceDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(RENOUNCE_DISCRIMINATOR);
}

export type RenounceInstruction<
  TProgram extends string = typeof SABLIER_LOCKUP_PROGRAM_ADDRESS,
  TAccountSender extends string | AccountMeta<string> = string,
  TAccountStreamData extends string | AccountMeta<string> = string,
  TAccountStreamNftMint extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSender extends string
        ? ReadonlySignerAccount<TAccountSender> &
            AccountSignerMeta<TAccountSender>
        : TAccountSender,
      TAccountStreamData extends string
        ? WritableAccount<TAccountStreamData>
        : TAccountStreamData,
      TAccountStreamNftMint extends string
        ? ReadonlyAccount<TAccountStreamNftMint>
        : TAccountStreamNftMint,
      ...TRemainingAccounts,
    ]
  >;

export type RenounceInstructionData = { discriminator: ReadonlyUint8Array };

export type RenounceInstructionDataArgs = {};

export function getRenounceInstructionDataEncoder(): FixedSizeEncoder<RenounceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RENOUNCE_DISCRIMINATOR })
  );
}

export function getRenounceInstructionDataDecoder(): FixedSizeDecoder<RenounceInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRenounceInstructionDataCodec(): FixedSizeCodec<
  RenounceInstructionDataArgs,
  RenounceInstructionData
> {
  return combineCodec(
    getRenounceInstructionDataEncoder(),
    getRenounceInstructionDataDecoder()
  );
}

export type RenounceAsyncInput<
  TAccountSender extends string = string,
  TAccountStreamData extends string = string,
  TAccountStreamNftMint extends string = string,
> = {
  /** Write account: the sender of the stream. */
  sender: TransactionSigner<TAccountSender>;
  /** Write account: the stream data account storing stream details. */
  streamData?: Address<TAccountStreamData>;
  /** Read account: the mint account for the stream NFT. */
  streamNftMint: Address<TAccountStreamNftMint>;
};

export async function getRenounceInstructionAsync<
  TAccountSender extends string,
  TAccountStreamData extends string,
  TAccountStreamNftMint extends string,
  TProgramAddress extends Address = typeof SABLIER_LOCKUP_PROGRAM_ADDRESS,
>(
  input: RenounceAsyncInput<
    TAccountSender,
    TAccountStreamData,
    TAccountStreamNftMint
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  RenounceInstruction<
    TProgramAddress,
    TAccountSender,
    TAccountStreamData,
    TAccountStreamNftMint
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SABLIER_LOCKUP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    sender: { value: input.sender ?? null, isWritable: false },
    streamData: { value: input.streamData ?? null, isWritable: true },
    streamNftMint: { value: input.streamNftMint ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.streamData.value) {
    accounts.streamData.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([115, 116, 114, 101, 97, 109, 95, 100, 97, 116, 97])
        ),
        getAddressEncoder().encode(expectAddress(accounts.streamNftMint.value)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.sender),
      getAccountMeta(accounts.streamData),
      getAccountMeta(accounts.streamNftMint),
    ],
    data: getRenounceInstructionDataEncoder().encode({}),
    programAddress,
  } as RenounceInstruction<
    TProgramAddress,
    TAccountSender,
    TAccountStreamData,
    TAccountStreamNftMint
  >);
}

export type RenounceInput<
  TAccountSender extends string = string,
  TAccountStreamData extends string = string,
  TAccountStreamNftMint extends string = string,
> = {
  /** Write account: the sender of the stream. */
  sender: TransactionSigner<TAccountSender>;
  /** Write account: the stream data account storing stream details. */
  streamData: Address<TAccountStreamData>;
  /** Read account: the mint account for the stream NFT. */
  streamNftMint: Address<TAccountStreamNftMint>;
};

export function getRenounceInstruction<
  TAccountSender extends string,
  TAccountStreamData extends string,
  TAccountStreamNftMint extends string,
  TProgramAddress extends Address = typeof SABLIER_LOCKUP_PROGRAM_ADDRESS,
>(
  input: RenounceInput<
    TAccountSender,
    TAccountStreamData,
    TAccountStreamNftMint
  >,
  config?: { programAddress?: TProgramAddress }
): RenounceInstruction<
  TProgramAddress,
  TAccountSender,
  TAccountStreamData,
  TAccountStreamNftMint
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SABLIER_LOCKUP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    sender: { value: input.sender ?? null, isWritable: false },
    streamData: { value: input.streamData ?? null, isWritable: true },
    streamNftMint: { value: input.streamNftMint ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.sender),
      getAccountMeta(accounts.streamData),
      getAccountMeta(accounts.streamNftMint),
    ],
    data: getRenounceInstructionDataEncoder().encode({}),
    programAddress,
  } as RenounceInstruction<
    TProgramAddress,
    TAccountSender,
    TAccountStreamData,
    TAccountStreamNftMint
  >);
}

export type ParsedRenounceInstruction<
  TProgram extends string = typeof SABLIER_LOCKUP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Write account: the sender of the stream. */
    sender: TAccountMetas[0];
    /** Write account: the stream data account storing stream details. */
    streamData: TAccountMetas[1];
    /** Read account: the mint account for the stream NFT. */
    streamNftMint: TAccountMetas[2];
  };
  data: RenounceInstructionData;
};

export function parseRenounceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedRenounceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      sender: getNextAccount(),
      streamData: getNextAccount(),
      streamNftMint: getNextAccount(),
    },
    data: getRenounceInstructionDataDecoder().decode(instruction.data),
  };
}
